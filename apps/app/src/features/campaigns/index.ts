// Bulk Messaging feature (frontend).
// Named `campaigns` internally — the user-facing name is "Bulk Messaging",
// kept distinct from Meta ad campaigns under `features/meta-campaigns`.
export * from './api';
export * from './channels';
export * from './components';
