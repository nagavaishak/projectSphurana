// Claire-guided onboarding feature (Typeform-style slide deck).
// NOTE: the legacy wizard under ./onboarding/, ./setup-profile/ etc. is NOT
// barreled here — it is imported directly by its routes and will be removed
// after the /welcome cutover.

// API hooks
export * from './api/index';

// Types
export * from './types';
