---
/**
 * Wireframe: Organisation landing.
 *
 * Static review page — see `components/wireframes/mock.ts`. Not linked from
 * anywhere in the product and safe to delete once the surface is built for real.
 */
export const prerender = false;

import { OrgLanding } from '@/components/wireframes/org-landing';
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout
  title="Organisation landing — wireframe"
  description="Static wireframe for review. Not a live page."
  theme="light"
>
  <OrgLanding client:load />
</BaseLayout>
