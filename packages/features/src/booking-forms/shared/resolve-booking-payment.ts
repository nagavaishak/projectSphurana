/**
 * Moved to `@borradh-workspace/web-shared` so the booking UI and the API share
 * ONE implementation — see the note there. Re-exported here so every existing
 * import and the test suite alongside this file are unchanged.
 */
export * from '@borradh-workspace/web-shared';
